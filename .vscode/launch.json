{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Wails: Debug KafkaTrail.app",
        "type": "go",
        "request": "launch",
        "mode": "exec",
        "program": "${workspaceFolder}/build/bin/KafkaTrail.app/kafka-trail",
        "preLaunchTask": "build",
        "cwd": "${workspaceFolder}",
        "env": {}
      }
    ]
  }
