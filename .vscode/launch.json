{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Wails: Debug kafka-trail.app",
        "type": "go",
        "request": "launch",
        "mode": "exec",
        "program": "${workspaceFolder}/build/bin/kafka-trail.app/kafka-trail",
        "preLaunchTask": "build",
        "cwd": "${workspaceFolder}",
        "env": {}
      }
    ]
  }
